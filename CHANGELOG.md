# 更新记录

## 0.1.12 — 语音输入、语音意图补全、AI 对话上下文与克制化工具栏

- **语音输入**：在 AI 菜单内点话筒 / 按右 `Alt` 即可录音；录制的是浏览器 `webm/opus`，客户端先解码为 16-bit PCM WAV 再交给语音识别，修复「说半天没识别」问题。新增 `POST /api/run19/asr`（`qwen3-asr`），密钥经 `resolveCredential`（插件会话 → 默认模型配置 → 环境变量 → `~/.dsh/.credentials.yaml`）解析，错误给出 `ASR_CREDENTIAL`/`ASR_PROVIDER` 等可读码。
- **语音意图补全**：转写后由 LLM 判断是否构成「完整可执行的编辑意图」，并给出改写/补全后的指令，自动统一繁体/简体（目标语言跟随系统 `navigator.language`）。**不自动提交**——识别文本先填入输入框由你确认。新增 `POST /api/run19/asr-polish`；对模型返回键名漂移做别名容错解析。该功能可在「⚙ 设置 → 语音输入」开关（默认开）。
- **AI 对话上下文**：同一份报告的 AI 编辑改为持久会话，携带历史对话上下文；每次编辑绑定 `reportId`，模型被锚定在当前报告上，`applyDocumentOperations` 若盖了别的 `reportId` 会被拒绝，防止误改其他报告。
- **入口与快捷键**：`//` 直接唤起唯一 AI 项；右 `Alt` 在 AI 菜单内切换录音；工具栏新增「进入 AI」「语音」按钮，与 `/`、`//`、选区编辑共用同一条 `propose → diff → apply/revert` 管线。AI 修改进行中会阻止切换报告（`aiBusy`）。
- **工具栏克制化**：视图与 AI/语音按钮改为中性底色 + 单色图标（去掉大红大绿与彩色 emoji），激活态为柔和主题色；唯一主色保留给「保存/重试」。建议菜单首帧闪烁修复。
- 测试：Host 路由基线新增 `asr`、`asr-polish`；`parity.test.mjs` 基线推进到 `a77ded0`。

## 0.1.11 — 生成进度卡 + 失败/手动关闭体验 + 审阅面板

- 「生成与分析」新增**生成进度卡**：初始化/检索规划/检索资料/获取数据/生成图表/AI 分析/组装正文/保存草稿/生成 PDF 逐步展示状态，支持失败与取消；成功后自动收起为紧凑头，失败/中止后可手动「关闭」，失败给出中文原因与修复提示（如 `SOURCE_FAILED` → 数据源配置缺失、`GENERATION_TIMEOUT` → 上游超时）。
- 底层为进度事件流：生成端点按步 `emit` 进度，生成接受可取消信号（`AbortSignal.any`），无需改硬编码流水线。
- **失败态 UI 修正**：不再出现「第 0 步（）失败」的误导标题，失败子步正确标记为失败、后续步跳过、已完成步保留，不再全部滞留「进行中」。
- 底部错误提示改为带样式的错误条：左侧警告徽标、右侧可手动关闭，避免无 UI 的一行红字。
- **审阅面板（证据进主内容区）**：点击「版本历史与差异 / 重点人工信息」等，证据在主内容区全宽展示，头部提供「← 返回正文」回到编辑器；面板打开时收起工具栏，避免遮挡。
- 设置弹窗改为真实嵌套 `<dialog>`，居中浮动且不再定位漂移。
- 可视化编辑器把最近一次 AI 请求的工具/调用次数/错误以状态条形式展示（无需 DevTools）。

## 0.1.9 — 租户 ID 可视化 + 冲突自动覆盖

- 设置面板新增「租户 ID」字段（可选，单租户可留空）：与地址/知识库/密钥一起可视化配置,`settingsView` 回显已存租户 ID,校验规则同知识库 ID(字母数字/下划线/横线);后端 `updateSettings`/连接器早已支持,本版把它接到界面。
- 保存与发布不再因「保存令牌冲突」中断单用户操作:客户端在自己的保存 / 发布确认里遇到 `CONFLICT` 时,自动取最新令牌并用**本地正文直接覆盖**一次,不丢本地输入。服务端令牌校验保留不变,仅客户端在本人操作里自动跟进——解决"没改其他配置却报冲突、无法发布"的问题。

## 0.1.8 — 设置面板措辞修正

- 徽标「WeKnora 已连接」改为「配置完整（未实测）」:该状态只校验本地配置完整性,不发起网络请求;真实连通性与发布身份以「测试连接」结果为准,避免"显示已连接却无法发布"的误解。

## 0.1.7 — 设置面板「测试连接」

- 设置面板新增「测试连接」:一键实测连接激活、读取检索(用配置的知识库实际列一条资料)与发布身份校验,逐项显示 ✓/✗ 及可读原因——发布身份 ✓ 即「确认发布」解锁条件,测试通过后主界面署名立即刷新,无需关闭面板重进。
- 常见失败给出中文提示(未配发布密钥、明文 http 限回环、知识库 ID 越权等);返回只含状态码和服务端确认的署名,不含密钥与响应体。

## 0.1.6 — WeKnora 可视化连接设置

- 工作台头部新增「⚙ 设置」：可视化配置 WeKnora 地址、知识库 ID、读取/发布密钥,保存即生效,无需重启或改配置文件。
- 密钥写入 `<插件目录>/secrets/` 受限文件(界面不回显、不进配置 JSON),沿用"配置只存密钥文件路径"的既有安全规则;发布密钥须与读取密钥分离。
- 知识库 ID 同时作为检索白名单与发布目标;环境变量 / Profile 显式配置仍优先于界面设置。
- 连接器改为配置齐备即自动激活,`CONNECTOR_ENABLED` 不再必需;新增 `settingsGet`/`settingsSave` Host 动作(仅人工点击可用)。

## 0.1.5 — Python 环境零配置

- 未显式配置 Python 时自动探测常见解释器（要求同时具备 Matplotlib 与 PyMuPDF），结果缓存到 `<插件目录>/python-runtime.json`;显式 env/按行配置仍最优先且不做校验。
- 全部候选不合格时自动创建托管 venv（`<插件目录>/venv`）并后台 `pip install matplotlib pymupdf`,尊重 `PIP_INDEX_URL`;进度与失败原因写入 `<插件目录>/python-setup.log`。
- 新装机器不再需要手工配置 Python 路径或预装依赖;仅在机器完全没有 Python 时需要先安装 Python 3.9+。

## 0.1.4 — 运行时配置与诊断修复

- `SOURCE_FAILED` 不再吞掉根因：错误消息带出内部机器码（如 `cause=GENERATION_FAILED`、`cause=GENERATION_TIMEOUT`、`cause=ENOENT`），stderr 与上游数据仍不对外暴露。
- `tokens-weekly-report-pdf` 行接受 `python` 作为 `pythonPath` 的别名，`tokens-weekly-report-source` 行反向接受 `pythonPath`；Profile 补丁用同一个键名即可同时配好两行,避免只覆盖生成器、漏掉 PDF 导致「PDF 生成失败」。
- 新增 `TOKENSCOWORK_WEEKLY_REPORT_ASSET_ROOTS`（逗号分隔）：把旧数据根目录加入 PDF 与 WeKnora 连接器的资产白名单,旧安装生成的草稿可以继续导出 PDF,不再报 `ASSET_OUTSIDE_WHITELIST`。
- README 补全 Python/资产根目录配置说明,并给出按行 id 覆盖（`- id:`,非 `- insert:`）的正确补丁示例。

## 0.1.3 — BlockNote 可视化编辑

- 可视化编辑使用 BlockNote，Markdown/PDF 实时浏览继续使用 CodeMirror；移除独立 Tiptap 编辑器入口。
- 支持六级标题、任务与嵌套列表、引用、代码、简单表格及图片说明/地址编辑；未修改 Markdown 片段保留原文。
- 修复正文滚动、链接弹窗裁切、斜杠菜单定位及嵌套自定义块保存丢失内容的问题。
- 扩充可新建的完整演示周报，包含离线图表、阅读预览和演示 PDF，保留已有草稿。
- React/React DOM 使用宿主 18.3.1，Mantine 锁定兼容版本，构建禁止夹带独立 React 运行时。

- 审阅操作切换忙碌/只读状态时保留编辑器实例与撤销历史；切换报告时重新建立独立历史。
- 保留原始交接文档与原有客户端契约测试路径，清理未使用的入口组件和样式。

验证：全量 116 项测试、构建、静态包检查及 npm 发布包清单检查通过。真实 DSH 输入法与长文性能仍需实际验收。

## 0.1.2 — 周报工作台 UI 更新

- 生成与正文编辑分步展示，增加原生侧栏入口、档案搜索和空白报告创建。
- 新增 Markdown 实时浏览：源码与阅读预览按段落双向同步滚动；编辑器支持 Markdown 语法高亮。
- 恢复左侧源码、右侧 PDF 浏览；两种浏览模式自动开启专注正文，并可手动退出。
- 工具栏新增 PDF 导出，当前稿保存且 PDF 同步后才可导出。
- 优化模板选择、表单对齐、明暗主题和窄屏布局；工作台背景不透明，输入控件仅保留单层焦点边框。
- 增加隔离的本地演示，可体验生成、编辑、PDF、版本及模拟发布，不调用真实数据或上传知识库。
- 保留 V1.1 后端、现有配置与数据目录、自动保存和冲突保护、人工确认发布及只读核对。

发布沿用统一包名 `@tokensapi/dsh-weekly-report`。通过插件市场安装的用户可检查更新，确认安装后重启客户端生效。开发目录链接不参与市场更新。

验证：构建、静态包检查、浏览器主题及分栏交互、Host/PDF/知识库模拟接口共 109 项测试。真实行情、AI 和知识库发布不属于本次验收范围。

## 0.1.1 — 合并上游周报插件 V1.1

以原交接 V1.0、TokensAPI 当前版和 V1.1 三方对比合并。npm 包名保持 `@tokensapi/dsh-weekly-report`；上游 V1.1 是交接版本名，对应本仓库适配版本 0.1.1。

交接包 `周报插件V1.1.zip` SHA-256：`bf69b071500130552a503b38b6a2b843151620c5ea9c64106a6a0d8f0bfcd655`。

| 层 | 合入内容 |
| --- | --- |
| 本地存储 report-core | 持久化商品品种，标题修改后仍可确定发布分类 |
| PDF report-pdf | 多行人工引用按空白归一化匹配，避免整段误加粗/下划线 |
| 界面与编排 report-review | 版本时间线及相邻差异、检索范围规划、材料核验与有限补查、检索理解展示 |
| 发布编排 report-review | 每报告独立目录；正文、资产、人工修改分层放置；移动结果核对；单知识文档的分块修订和累积人工审阅标记 |
| 知识库 weknora-connector | 按目录/时间枚举、限定知识 ID 检索、批量移动、分块修订读取、修正分块更新 URL、元数据 JSON 字符串化 |
| 数据获取 weekly-report-source | V1.1 相对 V1.0 无业务增量，保留 TokensAPI 的 Windows/UTF-8 适配 |

### 保留的 TokensAPI 适配

- 单包安装、现有 adapters、Host 注册 ID、私有 npm 地址及客户端模块 ID。
- Windows 绝对路径、密钥文件检查、JSON 原子存储兼容和 Python UTF-8 输出。
- 原有测试不因交接包没有携带测试而删除；前端由现有构建脚本重新生成。
- 显式配置的会话白名单继续有效；未配置白名单时支持已归档会话的本地报告读取。
- 同一版本已提交、结果未知或正在执行时继续阻止盲目重传；已提交的旧版本允许新版本的人类确认发布。
- 检索纠错按会话隔离，仅内存保存；时间线显示“已提交上传”，不将其当作解析或检索验收成功。
- 发布记录入口移入版本历史面板，保留只读核对能力。

### 发布后的目录

```text
商品策略/<商品分组>/周报/<reportId>/
├─ 报告正文
├─ 资产/       图片与 PDF
└─ 人工修改/   选中的公开人工资料
```

未配置分组的商品进入“未分类”并提示。已有本地 JSON 可以继续读取；没有品种字段的旧报告由标题推导。

### 验证范围

保留原有 97 项回归，并增加版本时间线、分块映射、目录分层、归档会话读取、检索纠错隔离及本地 HTTP 接口契约测试。使用本地模拟服务验证 WeKnora，不代表真实服务器发布成功。5100 真实数据服务和桌面 GUI 仍需在安装环境验收。
